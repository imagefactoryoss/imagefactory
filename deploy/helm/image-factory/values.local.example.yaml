app:
  # Generate with: openssl rand -base64 32 | tr -d '\n'
  encryptionKey: "REPLACE_WITH_BASE64_32_BYTE_KEY"

# Optional: keep this only if your cluster pulls from a private registry.
imagePullSecrets:
  - name: registry-credentials
