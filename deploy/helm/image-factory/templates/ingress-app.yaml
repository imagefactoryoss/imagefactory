{{- if and .Values.ingress.enabled .Values.frontend.enabled .Values.ingress.appHost }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "image-factory.fullname" . }}-app
  labels:
    {{- include "image-factory.labels" . | nindent 4 }}
  {{- if or .Values.ingress.annotations .Values.ingress.appAnnotations .Values.ingress.certManager.enabled }}
  annotations:
    {{- if .Values.ingress.certManager.enabled }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.certManager.clusterIssuer.name | quote }}
    {{- end }}
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.ingress.appAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.appHost | quote }}
      http:
        paths:
          {{- range .Values.ingress.backendPaths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "image-factory.fullname" $ }}-backend
                port:
                  number: {{ $.Values.backend.service.port }}
          {{- end }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "image-factory.fullname" $ }}-frontend
                port:
                  number: {{ $.Values.frontend.service.port }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
