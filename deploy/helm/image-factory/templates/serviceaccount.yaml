{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "image-factory.serviceAccountName" . }}
  labels:
    {{- include "image-factory.labels" . | nindent 4 }}
{{- end }}
